name: Validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Syntax-check bootstrap.sh
        run: bash -n bootstrap.sh

      - name: Run bootstrap.sh validation
        run: ./bootstrap.sh --validate
